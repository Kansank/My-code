extends layout

block content
  h1= title
  p Welcome to #{title}
   if user
    p #{user.username}
    p #{user.name}
    a(href="/logout", class="btn btn-link") Logout
   else
    .row
     .col-12
      a(class="btn btn-link", href="/login") Please Login
